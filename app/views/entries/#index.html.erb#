<h1>Listing entries</h1>

<table>
  <thead>
    <tr>
      <th>概要</th>
      <th>仕事の内容</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @entries.each do |entry| %>
      <tr>
        <td><%= entry.title %></td>
        <td><%= entry.body %></td>
        <td>
	  <%= link_to '詳細', entry %></td>
        <td><%#= link_to 'Edit', edit_entry_path(entry) %></td>
        <td><%#= link_to 'Destroy', entry, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
<!-- 企業のみ案件の追加できる -->
<% if company_signed_in? -%>
  <%= link_to '案件登録', new_entry_path %>
<% end -%>
